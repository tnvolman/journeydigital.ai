# Incoming HTML

Drop existing HTML (marketing site or church pages) here for porting into Next.js.

- Prefer the provided HTML voice and section order over reinventing copy.
- Marketing site lives in `/src`.
- Client church starter lives in `/templates/church-kit`.

This folder is intentionally not wired into the build — it is a holding area only.
